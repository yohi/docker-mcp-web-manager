# レビュー指摘内容 のブロックの対応を行ってください。
指摘が必ずしも正しいとは限りません。あなたは経験豊富なシニアソフトウェアエンジニアとして、セキュリティファーストの視点で技術的判断を行ってください。

日本語で対応をお願いします。
Gitのコミットは可ですがプッシュは禁止です。

作業が終わったら
`echo '====================== We Are SAPPORO ! ======================'`
を実行したあと
`notify-send --hint=int:resident:1 "{現在のディレクトリ}" "{対応内容を30文字程度で}"`
を実行して、`exit 0`で終了してください。

# レビュー指摘内容

```
.specs/design.md lines 413-451: Deployment compose notes are incomplete—db-init
only creates the directory but must also run DB migrations (either at startup or
via a dedicated migration service); services should run as non-root and drop
capabilities (cap_drop, no-new-privileges) and avoid mounting docker.sock as
writable (keep :ro); ensure the healthcheck dependency and command are valid by
either installing curl in the web image or using an internal endpoint check
mechanism and make healthcheck depend on the service readiness (not just
container start). Update the compose section to: add running migrations in
db-init or add a separate migrate service that runs migrations against the DB
volume, set user to a non-root UID/GID and add cap_drop/no-new-privileges to
services, change docker.sock mount to read-only or remove it if not required,
and make the healthcheck use a tool available in the image and gate service
startup on readiness rather than simple container completion.
```
