# レビュー指摘内容 のブロックの対応を行ってください。
指摘が必ずしも正しいとは限りません。あなたは経験豊富なシニアソフトウェアエンジニアとして、セキュリティファーストの視点で技術的判断を行ってください。

日本語で対応をお願いします。
Gitのコミットは可ですがプッシュは禁止です。

作業が終わったら
`echo '====================== We Are SAPPORO ! ======================'`
を実行したあと
`notify-send --hint=int:resident:1 "{現在のディレクトリ}" "{対応内容を30文字程度で}"`
を実行して、`exit 0`で終了してください。

# レビュー指摘内容

```
In .specs/design.md around lines 174 to 207, the spec declares types
(SecretReference, Resource, Prompt, ResourceLimits, NetworkConfig, JSONSchema)
and references (e.g. ServerConfiguration.secrets and
MCPServer.resources/prompts) that are not defined and have no DB-schema mapping;
add concise definitions for each missing type including fields and types,
describe JSONSchema structure, and update the design to show how each maps to
persistent storage (tables/collections, FK relations or join tables for
secrets/resources/prompts), include a Secrets table or SecretReferences table
linked to ServerConfiguration.serverId, define schema for ResourceLimits and
NetworkConfig and whether they are embedded or normalized, and ensure all
names/IDs used in interfaces match column names in the DB section and mention
migrations needed to create the new tables/columns.
```
