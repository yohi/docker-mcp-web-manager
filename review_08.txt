# レビュー指摘内容 のブロックの対応を行ってください。
指摘が必ずしも正しいとは限りません。あなたは経験豊富なシニアソフトウェアエンジニアとして、セキュリティファーストの視点で技術的判断を行ってください。

日本語で対応をお願いします。
Gitのコミットは可ですがプッシュは禁止です。

作業が終わったら
`echo '====================== We Are SAPPORO ! ======================'`
を実行したあと
`notify-send --hint=int:resident:1 "{現在のディレクトリ}" "{対応内容を30文字程度で}"`
を実行して、`exit 0`で終了してください。

# レビュー指摘内容

```
.specs/requirements.md around lines 88 to 96: the import/export acceptance
criteria lack requirements for schemaVersion handling, compatibility, dry-run
validation, size/item limits, and stricter masking of sensitive fields; update
the spec to require (1) explicit schemaVersion in exported files and validation
on import with defined backward- and forward-compatibility rules (accept
older/newer versions per documented migration/compatibility policy or reject
with clear error), (2) a dry-run mode that validates the file fully and
simulates applying changes without persisting them, returning detailed
validation results, (3) enforced file size and maximum item/count limits with
explicit rejection behavior, and (4) a stricter masking/exclusion policy for
sensitive fields on export and verification that imports do not reintroduce
unmasked secrets—add acceptance tests and error messages for each case.
```
